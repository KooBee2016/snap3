#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ga-analytics/bin/ga-analytics" "$@"
  ret=$?
else 
  node  "$basedir/../ga-analytics/bin/ga-analytics" "$@"
  ret=$?
fi
exit $ret
